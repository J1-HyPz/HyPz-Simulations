# Copy to .env next to docker-compose.yml. Never commit the real file.
# Free account: https://dashboard.api-football.com  (100 requests/day)
# Absent or empty is fine - lineups are simply skipped and everything else runs.
API_FOOTBALL_KEY=
